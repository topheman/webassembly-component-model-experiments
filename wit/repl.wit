package myrepl:repl

import myrepl:host/plugin-runner.{repl-transport.{repl-result, repl-status}}

interface repl {
  /// Called by the host when a user submits a command line
  readline: func(line: string) -> repl-result
}

world repl {
  import plugin-runner
  export repl
}
