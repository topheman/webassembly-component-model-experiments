interface transport {
  record repl-result {
    color: option<string>,
    status: repl-status,
    output: option<string>,
  }

  enum repl-status {
    success,
    error,
    warning,
  }

  record readline-result {
    command: string,
    payload: string,
  }

  record plugin-config {
    command: string,
    arg-count: option<s8>,
    man: string,
  }

  record repl-env-var {
    key: string,
    value: string,
  }
}
